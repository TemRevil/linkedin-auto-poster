# LinkedIn Auto-Poster — Claude Code Config

## Git Identity
Always use this identity for every commit in this repo:
```
git config user.name "TemRevil"
git config user.email "temrevil@gmail.com"
```
Run these at the start of every session before making any commits.

## Branch
Default development branch: `claude/bold-bardeen-AvCF2`
Push target: `origin claude/bold-bardeen-AvCF2`

## Commit style
- Conventional commits: `fix:`, `feat:`, `refactor:`, etc.
- No AI-attribution trailers (no `Co-Authored-By` lines)
- Author must always be TemRevil (set via git config above)
